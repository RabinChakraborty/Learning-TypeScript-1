let num: number = 20;
console.log(num);
//First typeScript program learned to assign a number variable.

//2.debug typescript in vsCode.
// add this line below program while debug in launch.json="preLaunchTask":"tsc:build - tsconfig.json"
if (num < 50) {
  console.log('hello');
}
